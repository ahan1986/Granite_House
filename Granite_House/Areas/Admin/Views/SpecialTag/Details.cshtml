@*because we don't need a list of the data, we don't need to set this up as IEnumerable<>. We will be dealing with single Granite types*@
@model Granite_House.Models.SpecialTag
@{
    ViewData["Title"] = "Details";
}

<h2>Details Product Type</h2>
<br />
<h2 class="text-info"> Details Product Type </h2>

@*There won't be form needed because we are not going to be POSTing anything to the backend*@

<div class="p-4 border rounded">
    @*added this hidden input and have asp-for to use the int id in the Edit method so that when it get's posted we will have the id*@

    <div class="form-group row">
        <div class="col-2">
            @*when you use asp-for. It will bind the Name to productsTypes name property.*@
            <label asp-for="Name"></label>
        </div>
        <div class="col-5">
            @*disabled will make it so that the user cannot type anything into the input box*@
            <input asp-for="Name" disabled class="form-control" />
        </div>
    </div>
    <div class="form-group">
        @*we didnt reference area because we are in the same area*@
        <a asp-action="Edit" asp-route-id1234="@Model.Id" class="btn btn-success">Edit</a>
        <a asp-action="Index1" class="btn btn-success">Back To List</a>
    </div>
</div>

